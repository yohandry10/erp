# Estado Actual del ERP

<!-- DOC-NAV:START -->
> Navegacion documental: primero lee `docs/START_HERE.md`. Estado vivo: `docs/00_coordination/CURRENT_STATE.md` y `docs/00_coordination/FLOW_STATUS.md`. Mapa completo: `docs/DOC_NAVIGATION_MANIFEST.md`.
>
> Rol de este archivo: `estado_vivo`.
>
> Leer tambien: `docs/START_HERE.md`, `docs/DOC_NAVIGATION_MANIFEST.md`.
>
> Regla: si este documento contradice codigo verificado o docs canonicos, prevalecen codigo actual + `START_HERE` + `CURRENT_STATE` + `FLOW_STATUS`.
<!-- DOC-NAV:END -->

Fecha de actualizacion: 2026-07-24 (cierre QA funcional, numeración fiscal y RBAC)

- **Cierre QA funcional DEV 2026-07-24:** se revalidaron los verticales Compras, Inventario/Logística, POS/Cajas, Ventas, CPE/GRE/SIRE, RRHH, Contabilidad, Finanzas y Auth/Auditoría/RBAC/RLS con escrituras sintéticas controladas. Backend Jest quedó 120/120 suites y 1106/1106 tests; Web type-check, build Next 111/111, smoke UI 148/148, contrato de tema 2/2 y calidad UI 4/4 quedaron limpios. `353` alinea `clientes.nombre_comercial` y CxC atómica; `354` unifica la secuencia fiscal B/F entre POS y Documentos sin renumerar históricos; `355` impide que ADMIN de un tenant normal herede los cinco recortes de ADMIN demo. Se cerraron también trailing slash de `/login/`, sidebar móvil visible durante hidratación, persistencia/contraste de tema y estados/QA de Offline. `347..355` permanecen sólo en DEV y PROD no fue consultada ni modificada. Evidencia: `docs/audits/2026-07-24-production-closure-functional-qa.md`.
- **Liquidación de beneficios sociales corregida 2026-07-26:** la CTS se pagaba **doce veces por debajo** de lo legal (`(RC/360) * dias` con días ya expresados en dozavos): un empleado con 12 meses y sueldo S/ 1,300 recibía S/ 126.39 en vez de S/ 1,516.67. En la misma liquidación: vacaciones sin prorratear (30 días desde el primer día de trabajo), indemnización por despido sin el tope de 12 remuneraciones del art. 38 D.S. 003-97-TR, gratificación trunca y bonificación 9% (Leyes 27735 y 30334) ausentes, y fechas corridas un día por parsear `YYYY-MM-DD` como UTC en `America/Lima`. Los cálculos se extrajeron a `liquidacion-peru.util.ts` (módulo puro) con 20 specs que citan la norma. Verificación end-to-end contra la API con empleado real creado y borrado: CTS 1,516.67, vacaciones 1,300.00, gratificación trunca 236.17, indemnización 1,950.00, total 5,002.84, todas coincidiendo con el cálculo legal. Backend 125/125 suites y 1185/1185 tests. **Sigue sin implementarse** lo periódico: gratificaciones de julio y diciembre como concepto de planilla, depósito semestral de CTS y remuneración vacacional al goce; sin eso el módulo no opera una planilla peruana completa. Evidencia: `docs/audits/2026-07-26-rrhh-liquidacion-beneficios-sociales.md`.
- **Pendientes declarados del barrido POS cerrados 2026-07-26:** se corrigieron los siete hallazgos que quedaban abiertos. `PlanillasService` devolvía 500 en guardas de negocio: ahora 409 en conflictos de estado (recalcular planilla ya calculada, pagar una no calculada, generar asientos con estado incompatible), 404 en planilla inexistente y 400 en precondiciones; los doce `Error` restantes son fallos de infraestructura y siguen en 500. `contratoVigenteDe` desempata por fecha de inicio más reciente, así que una renovación con dos contratos vigentes ya no deja el sueldo al orden de la base. Las etiquetas sin `htmlFor` eran transversales (442 en 83 archivos): un codemod conservador asoció 192 en 21 archivos, incluidos los 14 de `EmpleadoModal`, y quedan 250 declarados como ambiguos. Los chips de categoría del POS cuentan sobre el catálogo vendible y no sobre el completo. El panel de apertura de caja dejó de aparecer una pantalla por debajo de la tarjeta. `360__metodos_pago_tipo_check_y_fallback_global.sql` cierra los dos huecos de `359`: los 4 tenants sin métodos propios ya resuelven contra el catálogo global en vez de caer a `EFECTIVO`, y `tipo` tiene CHECK de taxonomía. El reporte de arqueo imprime "sin arqueo" en cierres administrativos en vez de un conteo cero que nunca se hizo. Backend 124/124 suites y 1165/1165 tests; build Next 111/111. `360` aplicada sólo en DEV. Evidencia: `docs/audits/2026-07-26-cierre-de-pendientes-declarados.md`.
- **Sesión de caja y cierre sin arqueo corregidos 2026-07-26:** `GET /api/pos/sesion-caja` cerraba toda sesión abierta cuya apertura no cayera en el día **UTC** actual, sin saldo teórico, sin conteo, sin `cerrado_por` y sin marcar cierre administrativo: abrir la pantalla del POS bastaba para cerrar la caja del turno anterior y borrar el rastro del efectivo. Evidencia en DEV: sesión con 11 ventas y S/ 214.51 esperados cerrada con esperado 0 / contado 0 / diferencia 0; otra sesión con S/ 2,653.23 en movimientos, igual. Agravado porque el backend comparaba el día en UTC y la UI en hora local, lo que con Perú en UTC-5 parte la jornada a las 19:00 y hacía que el POS mostrara "CAJA CERRADA" con la sesión abierta en BD, empujando al cajero a abrir otra. Se corrigió: el GET ya no muta estado, la UI reconoce la sesión abierta aunque sea de ayer, y el cierre administrativo legítimo de `cajas.service.ts` registra el saldo teórico con la fórmula del cierre con arqueo. Límite declarado: el trigger `app.normalize_sesiones_caja_row` fuerza `monto_contado` a 0 y deriva la diferencia, así que "no contado" no puede distinguirse de "contado cero"; la sesión queda como descuadre visible en vez de cierre limpio. Verificado en navegador integrado: caja de ayer reconocida y operativa, GET que no la cierra, y cierre administrativo con `monto_esperado = 79.50`. Backend 124/124 suites y 1162/1162 tests. Evidencia: `docs/audits/2026-07-26-caja-sesion-cruce-de-dia-y-cierre-sin-arqueo.md`.
- **Arqueo de caja y medios de pago corregidos 2026-07-26:** un pago mixto (S/ 20.00 efectivo + S/ 9.50 tarjeta) acreditaba la gaveta por los S/ 29.50 completos, dejando un faltante fantasma en el arqueo por cada venta con medio distinto al efectivo. La lógica de caja era correcta en RPC y servicio; el defecto estaba en el dato: `DemoService.seedMetodosPago` insertaba los métodos sin `tipo` y la columna tiene `DEFAULT 'EFECTIVO'`, así que 26 tenants de DEV tenían TARJETA/TRANSFERENCIA/YAPE tipificados como efectivo. Se corrigió el seed con la taxonomía del catálogo global de `024`, `359__metodos_pago_tipo_por_codigo.sql` realineó 78 filas por tenant, y la clasificación de crédito pasó a una lista explícita de medios inmediatos para que corregir el dato no abriera CxC por ventas con Yape o transferencia ya pagadas. Verificación: mismo pago mixto repetido deja movimiento de caja 20.00 y 0 CxC; backend 124/124 suites y 1162/1162 tests. Riesgo latente declarado: el `DEFAULT 'EFECTIVO'` sigue sin CHECK y un tenant sin filas propias cae al mismo default en la RPC. `359` aplicada sólo en DEV. Evidencia: `docs/audits/2026-07-26-pos-metodos-pago-tipo-y-arqueo.md`.
- **Total cobrado en POS alineado con el comprobante 2026-07-26:** dos defectos independientes hacían que el importe cobrado no coincidiera ni con la pantalla ni con el CPE. (1) `descuento_global` no entraba al recálculo autoritativo: la pantalla y el modal decían "Cobrar S/ 7.02" y la venta quedaba registrada en S/ 7.67; el frontend además lo restaba después del IGV en vez de reducir la base. (2) `pos_registrar_venta_full_tx_legacy_327`, que es la RPC que realmente inserta la venta, liquidaba IGV plano sobre todo el subtotal: una boleta de un bien exonerado se cobraba en S/ 5.90 mientras el CPE se emitía por S/ 5.00. Se corrigió en las tres capas: el backend prorratea el descuento global sobre la base imponible y reparte el IGV por ítem según Catálogo 07, el frontend calcula IGV sólo sobre base gravada, y `358__pos_igv_por_afectacion_en_venta.sql` hace que ambas RPC sumen el IGV declarado por ítem con la tasa plana sólo como compatibilidad. Verificación con tres escenarios reales (descuento 10%, exonerado y mixto) donde pantalla, `ventas_pos` y `cpe_data` coinciden; backend 124/124 suites y 1160/1160 tests; type-check API y Web limpios. `358` aplicada sólo en DEV. Evidencia: `docs/audits/2026-07-26-pos-descuento-global-e-igv-por-afectacion.md`.
- **Correlativo fiscal del POS unificado 2026-07-26:** la auditoría de colisiones históricas B/F que `FLOW_STATUS` mantenía abierta se ejecutó en DEV: 0 números fiscales duplicados en `documentos`, `cpe` y `ventas_pos`, pero 3 series con el contador canónico por debajo del máximo ya emitido. Causa: `354` redirige a `documento_series` sólo cuando la serie del ticket ya es fiscal; si el cliente no envía `comprobante.serie` el backend usa `T001` por defecto y el CPE heredaba ese correlativo interno, emitiendo en la serie fiscal sin reservar. Colisión reproducida con `ROLLBACK`: la secuencia canónica reentregaba `B001-00000009` y `00000010`, ya emitidos. `pos.service.ts` ahora reserva el correlativo en `documento_series` cuando el ticket usa serie interna, conserva el del ticket cuando ya es fiscal y reutiliza el reservado en reintentos. `357__fiscal_correlativo_resync_documento_series.sql` realinea contadores al máximo emitido sin renumerar históricos; aplicada sólo en DEV. Verificación: venta real por navegador `B001-00000011` con el contador de tickets desalineado a 500, venta por API sin serie con ticket `T001-00000501` y comprobante `B001-00000012`, backend 124/124 suites y 1157/1157 tests, type-check API limpio. PROD no fue consultada ni modificada. Evidencia: `docs/audits/2026-07-26-pos-fiscal-correlativo-single-sequence.md`.
- **Arquitectura de estilos cerrada 2026-07-16:** `globals.css` bajo de 2546 a 193 lineas y quedo limitado a directivas Tailwind, tokens shadcn y base. Las clases legacy de dashboard y los neutrales literales del puente fueron migrados a utilidades Tailwind/tokens semanticos; `styles/dashboard-primitives.css` y `styles/theme-compat.css` se eliminaron y ya no hay imports compatibles. Tambien se corrigieron 1343 utilidades numericas que Tailwind ignoraba (`text-3`, `rounded-2`, etc.) y el gate exige que permanezcan en 0. Se centralizo el tema en `use-dashboard-theme`, se agrego `components.json`, se migraron modales a shadcn/Radix y se eliminaron todos los `!important` (102 -> 0). Gates: cuatro codemods idempotentes, type-check, build web 111/111, auditor UI sin criticos, Playwright de contrato 2/2 y smoke renderizado 14/14 dark/light sin superficies incompatibles, `pageerror` ni errores de consola. Fuente: `docs/architecture/FRONTEND_STYLING_ARCHITECTURE.md`.
- **Refactor estructural CPE 2026-07-15:** `cpe.service.ts` bajó de 3556 a menos de 1000 líneas y quedó como fachada. Se separaron UBL 2.1, certificados, entrega OSE/SUNAT, anulación/reversos, reportes, sincronización con Documentos y registro de XML firmado. Se preservaron contratos públicos y los invariantes `01/03/07/08`, guard PFX/RUC, idempotencia y trazabilidad contable. Verificación: typecheck, lint CPE sin advertencias y API Jest 118/118 suites, 1085/1085 tests. Fuente: `docs/architecture/CPE_SERVICE_BOUNDARIES.md`.
- **Refactor estructural Contabilidad 2026-07-15:** el controlador de 3114 líneas se convirtió en un barrel y sus 63 rutas quedaron distribuidas en 7 controladores por recurso: períodos, presupuestos, centros de costo, estados financieros, asientos, libros y eventos. Se conservaron prefijo, verbos, rutas, DTO, guards y permisos; ningún controlador resultante supera 1000 líneas. Verificación: 63/63 rutas únicas, matriz de permisos 63/63 actualizada, typecheck, lint sin advertencias, build y API Jest 118/118 suites, 1085/1085 pruebas. Fuente: `docs/architecture/CONTABILIDAD_CONTROLLER_BOUNDARIES.md`.

Este documento es la fuente canonica de estado vivo. La primera lectura de una sesion nueva es `docs/START_HERE.md`, que explica el orden de lectura, jerarquia documental y rutas por tarea. Este archivo no reemplaza las auditorias, manuales ni reportes; fija la linea vigente.

## Lectura obligatoria al iniciar

1. `docs/START_HERE.md`
2. `docs/00_coordination/CURRENT_STATE.md`
3. `docs/00_coordination/FLOW_STATUS.md`
4. `docs/00_coordination/AGENT_SYNC.md`
5. `docs/DOC_NAVIGATION_MANIFEST.md` si la tarea requiere revisar documentacion o decidir fuentes.
6. Documento fuente del dominio que vas a tocar, segun `docs/START_HERE.md`.

Si la sesion toca base de datos, tambien consultar los artefactos baseline listados en `AGENTS.md` antes de borrar, reconstruir o aplicar migraciones.

## Estado ejecutivo

- **Limpieza estructural 2026-07-15/16:** se eliminaron 27 declaraciones de dependencias no usadas y 13 archivos huerfanos; 3 paquetes pasaron de runtime a desarrollo y 2 dependencias de tooling quedaron explicitadas. Knip de produccion queda limpio; el unico aviso general es el binario externo `helm`. Tras refactorizar CPE, Contabilidad y CSS global quedan 47 archivos mayores a 1.000 líneas: 30 runtime, 7 tests, 6 migraciones vigentes y 4 artefactos legacy/rebuild. Los hotspots restantes y dos ciclos reales (Events/Outbox y Auth/permisos/tenant) están priorizados en `docs/audits/2026-07-15-dependency-and-large-file-cleanup.md`. No se tocaron bases ni migraciones aplicadas.
- **Cierre integral UI/contabilidad/seguridad 2026-07-15:** los hallazgos externos fueron contrastados con codigo y pruebas. Se corrigieron superficies dark/light, exportacion Analytics, seed demo/certificado `bytea`, sesion demo revocable y trazabilidad `CPE.event_id -> outbox -> asiento.source_event_id`. Smoke visual/storage: 18/18 combinaciones, 0 errores de pagina/consola y 0 secretos; E2E Analytics 2/2, CPE 1/1 y verticales Compras/Contabilidad/POS 3/3; API Jest 118 suites/1085 tests; typecheck, lint sin errores, builds, gate UI y encoding OK. Solo DEV `hbueraexcbowpfnjlppi`; PROD no fue tocada. Fuente: `docs/audits/2026-07-15-ui-accounting-security-closure.md`.
- **Corrección de anulación fiscal POS 2026-07-24:** el guard CPE conserva la identidad de `factura.emitida`, pero si el asiento fue creado antes por `venta.procesada` lo resuelve por referencia fiscal canónica, dentro del tenant y exigiendo unicidad + detalle. `POST /documentos/:id/anular` delega los documentos con CPE a la anulación fiscal integral y falla cerrado si un documento fiscal emitido perdió el vínculo; la UI reemplazó `prompt()` por un diálogo accesible. E2E DEV desde tenant nuevo con dos ventas consecutivas: `B001-00000001` creó `BC001-1`, restauró stock `14 -> 15`, caja `-106.08` y asiento inverso `106.08 = 106.08`; `B001-00000002`, con `cpe.event_id != asiento.source_event_id`, se anuló sin 409, creó `BC001-2`, restauró Azúcar `119 -> 120` en existencia física y agregados, caja `-7.67`, evento `cpe.anulado` completed y asiento inverso confirmado de tres detalles con cuadre `7.67 = 7.67`. Typecheck API/Web OK; suite API 119/119 y 1095/1095. PROD no fue consultada ni modificada. Fuente: `docs/architecture/CPE_SERVICE_BOUNDARIES.md`.
- **Contrato de entornos cerrado 2026-07-14:** DEV `hbueraexcbowpfnjlppi` queda para desarrollo, QA y demos; PROD `wypnbcptofqdmoynlonq` queda solo para datos reales. Se respaldo y elimino de PROD la totalidad de 41 tenants QA/demo, 133 usuarios Auth y 49,613 filas tenant-scoped; conteos finales `tenants=0`, `auth.users=0`, `storage.objects=0`, filas con tenant no nulo `=0`. Migracion `346__deployment_environment_boundary.sql` aplicada en ambos proyectos; preflight DEV/PROD OK; validadores PROD: entorno 5/5, contabilidad 5/5, inventario 6/6, tesoreria 11/11. Fuente: `docs/architecture/ENVIRONMENT_DATABASE_BOUNDARIES.md` y `docs/audits/2026-07-14-prod-demo-data-cleanup.md`.
- **Inventario single-ledger y UX POS cerrados en DEV 2026-07-22:** se confirmó el doble saldo previo de POS y se aplicaron `347..352` sólo en DEV. `producto_existencias` es la fuente física por almacén; `productos.stock_*` y `producto_stock_sucursal` son derivados protegidos. POS deriva el almacén desde la caja, no tiene fallback API legacy y el puente interno `327` está revocado/encapsulado en la misma transacción. Evidencia: venta API `15->14`, retry idempotente, concurrencia 1 éxito/1 rechazo, contrato 6/6 y pruebas visibles. La UX final usa dos zonas catálogo/venta, modo caja opaco, atajos F2/F4/F8, filtros adaptables y tarjetas empresariales neutras/compactas con `auto-fit`: sin placeholders grandes, categorías superpuestas, paletas decorativas ni columnas reservadas vacías. Carrito, resumen y cobro quedan consecutivos; sólo la lista crece con scroll. El menú de caja es opaco y el cierre distingue la acción destructiva, muestra datos verificables y ya no calcula una diferencia falsa contra el monto inicial. Se corrigieron también el redondeo operativo a dos decimales y la vista previa histórica, que deriva `Boleta/Factura/Ticket`, muestra número real y nombres de producto. La impresión histórica ya no ejecuta `window.print()` sobre todo el ERP: vista previa e impresión comparten un documento aislado, 80 mm para ticket/boleta y A4 para factura. Existe vista previa de borrador sin emitir ni mutar datos. La prueba visible cargó cinco líneas con cantidades múltiples, validó ticket y factura completos por S/353.06, sin desborde horizontal; el frame de impresión contenía únicamente empresa, cliente, cinco productos y totales. La auditoría posterior del historial confirmó un tercer ticket DEV `B001-00000003` por S/113.75 con Audífonos y Azúcar; el stock visible quedó `14->13` y `119->118`. No se revirtió ni borró esa venta. Consola sin `warn/error` y sin desborde horizontal. La prueba transaccional previa confirmó `B001-00000002`, stock UI de Azúcar `120->119`, historial y temas dark/light. La demo sigue sin CAPTCHA, limitada a DEV por bandera, bloqueo duro PROD y throttle 5/h. PROD no fue tocada; promoción `347..352` sigue pendiente de runbook. Fuente: `docs/audits/2026-07-22-inventory-single-ledger-closure.md`.
- **Build Web revalidado 2026-07-22:** se corrigieron seis imports `react-hot-toast` mal posicionados en pantallas de Compras y dos dependencias de hooks en `RecepcionWizard`. Type-check Web, ESLint focalizado y build Next.js quedaron limpios; se generaron 111/111 páginas. Navegador integrado confirmó POS y `/dashboard/compras/recepciones` sobre la compilación final.
- **Persistencia local sensible cerrada 2026-07-14:** navegador y E2E ya no persisten JWT ni credenciales demo en `localStorage/sessionStorage`; Tauri guarda el Bearer con DPAPI en `auth_token.dat`; la outbox remueve headers sensibles y reinyecta el token actual solo al enviar. Se eliminaron estados E2E/reportes/cache con copias sensibles, el backup temporal de la purga PROD y el Web Storage residual del WebView. Los `.env` y PFX locales quedaron con ACL exclusiva de usuario actual/SYSTEM/Administradores. Fuente: `docs/security/session-auth.md` y `docs/audits/2026-07-14-local-secret-storage-hardening.md`.
- ERP listo a nivel de codigo core para candidato de produccion: type-check monorepo OK, pruebas focales criticas OK, audit prod npm limpio, migraciones `337..345` aplicadas/verificadas en DEV y PROD por `psql`; `342__sunat_tenant_onboarding_credentials.sql` ya esta simétrica en ambos entornos y cierra el contrato persistente de credenciales SUNAT por tenant. PROD (`wypnbcptofqdmoynlonq`) fue revalidado por conexion directa: Postgres 17.6, `342` con 15 columnas SUNAT tenant-level y 3 constraints validadas en `empresa_config`, `343` con locks RPC `SECURITY DEFINER` restringidos a `service_role`, `344` con `cuentas_por_cobrar.total` consistente con `monto_total`, y `345` con deuda critica Supabase Advisor cerrada en catalogo. No declarar produccion real absoluta sin certificado SUNAT/OSE productivo valido para el RUC, secretos productivos finales, email real si aplica y smoke externo autorizado.
- Desktop/Tauri queda listo a nivel de codigo para operar offline-first en los flujos controlables por codigo: SQLite local por tenant, outbox durable, snapshots/cache binario por tenant, escrituras genericas local-first para modulos restantes, fiscal local con correlativos por tenant, SIRE local filtrado por tenant, DPAPI/redaccion de secretos locales y runtime sin shell capability. Verificado el 2026-06-03 con `tauri:build`, `test:offline`, type-check web/backend, `cargo check` y `git diff --check`. Siguen fuera del codigo: primera autenticacion sin sesion previa, certificado/credenciales SUNAT/OSE productivos, CDR/aceptacion productiva, RA/RC/GRE externos segun alcance y smoke con API real desde el `.exe`.
- Navegacion documental anti-duplicacion consolidada el 2026-06-04: toda sesion debe leer `START_HERE`, `CURRENT_STATE`, `FLOW_STATUS`, `AGENT_SYNC`, `ANTI_DUPLICATION_PROTOCOL`, `DECISIONS` y luego el manifiesto/fuente del dominio. Objetivo: no repetir auditorias, no reimplementar codigo existente y no revivir falsos positivos historicos.
- Alcance comercial inicial fijado a Peru/SUNAT: login, wizard, creacion/edicion de tenants, catalogo de paises y validaciones de proveedores operan solo con `PE/pais_id=1/PEN`; CO/CL/MX/EC quedan como roadmap hasta validacion fiscal/legal/E2E propia.
- Readiness base: Gate 21/22 documentado al 2026-05-16 en `docs/production-readiness/ERP_PRODUCTION_READINESS.md`.
- Despues del corte de readiness existen auditorias forenses y migraciones posteriores. El head documental actual debe considerar `327..345`.
- Auditoria multiusuario/performance aplicada el 2026-05-26: retries frontend no idempotentes desactivados para escrituras, polling visible/sin solapes, banners de configuracion cacheados, workers cron protegidos con locks distribuidos y banderas de apagado para pruebas read-only. Prueba autenticada read-only contra API local + Supabase real: 589/589 OK, 0 errores, 0 HTTP 429/5xx, p95 1490 ms, p99 1956 ms. Se corrigio `GET /api/finanzas/cxp?limit=10&page=1` para aceptar paginacion. Outbox remoto quedo limpio: `completed=3985`, `pending=0`, `processing=0`, `failed=0`, `dead_letter=0`.
- El worktree contiene muchos cambios previos del usuario/de sesiones anteriores. No revertir ni stagear archivos por inercia.
- Fase 1A de hardening pre-prod aplicada el 2026-05-26 sobre el reporte forense `docs/audits/2026-05-26-forensic-audit-pre-prod.md` (post-triage con Codex):
  - **C-001** (`compras.controller.ts:crearProveedor`): refactor a `CreateProveedorDto` + `@CurrentTenant()`. Quitado el fallback al primer tenant y la lectura de `tenant_id` del body. Check de RUC duplicado ahora filtra por `tenant_id`.
  - **C-002** (`shared/integration/accounting-entries.service.ts:calcularCostoVentas`): firma cambiada a `(items, tenantId)`. Query `productos` filtra por `tenant_id`. Caller `procesarAsientoVenta` pasa `venta.tenantId`.
  - **H-001** (`modules/ventas/clientes/clientes.service.ts:delete`): queries de dependencias en `cotizaciones` y `pedidos_venta` ahora incluyen `.eq('tenant_id', tenantId)`.
  - **C-003 + H-004..H-007**: nuevo util `apps/erp-api/src/common/util/postgrest.util.ts:sanitizePostgrestSearch` con allowlist conservadora. Aplicado en 5 sitios con `.or(...ilike%${input}%...)`: `compras/repositories/proveedores.repository.ts`, `contabilidad/services/plan-cuentas.service.ts:buscar`, `usuarios/user-management.service.ts:findAll`, `tenants/tenant-management.service.ts:listTenants`, `notifications/inventory-stock-alerts.listener.ts:verificarNotificacionReciente`.
  - Verificación post-fix: `tsc --noEmit` limpio en mis archivos. Suite jest backend 994/995 OK en ese corte (el fallo `cpe-integration.verify.spec.ts` era pre-existente en esa fecha y quedo cerrado el 2026-06-16 durante el hardening SUNAT). +11 tests netos: 8 nuevos para `sanitizePostgrestSearch` + 3 que aparecieron en paralelo.
  - Fase 1B cerrada por `338/339/341`; los remanentes de Fase 2 son deuda de mejora/observabilidad, no bloqueantes de codigo core.
- Codex consolido Fase 1A + modulo migration completo en commit `5c35d76 fix(preprod): harden forensic audit findings` (rama `codex/accounting-production-closure`, 2026-05-27). Verificado que C-001/C-002/H-001/sanitizador sobreviven intactos.
- Fase 1B cerrada (atomicidad fiscal). **C-004 CERRADO 2026-05-27**: nueva migración `338__cerrar_recepcion_transaccional.sql` con RPC `cerrar_recepcion_tx(p_recepcion_id, p_tenant_id, p_user_id, p_observaciones)`. El cierre de recepción ahora es atómico (todo-o-nada): ingreso de stock por item (reusa `registrar_movimiento_almacen` vía llamada en la misma transacción), `cantidad_recibida` en detalles de OC, recálculo de estado de OC y cierre de recepción corren en una sola transacción con ROLLBACK total si algo falla. La RPC retorna jsonb con los movimientos creados y el backend re-emite `MovimientoStockEvent` POST-COMMIT (preserva el asiento contable de entrada Mercaderías/Cargas). `recepciones.service.ts:cerrarRecepcion` refactorizado: reemplaza el loop JS por una llamada a la RPC; se limpiaron huérfanos resultantes (`InventarioService` inyección, `actualizarEstadoOrden`, import `CalidadRecepcion`). Verificación: migración aplicada a DEV; smoke SQL real OK (recepción→CERRADA, orden→PARCIAL, detalle→7, stock→7, existencias→7, 1 movimiento); specs reescritos (recepciones.service 18/18 + recepciones-inventario-integration 3/3); tsc limpio; suite backend 991/992 en ese corte (el fallo `cpe-integration.verify.spec.ts` era pre-existente y quedo cerrado el 2026-06-16). Migración 338 aplicada a DEV **y PROD** (PROD verificado 2026-05-29: `cerrar_recepcion_tx` presente, EXECUTE solo `service_role`).
- **H-002 (facturar pedido) — RECLASIFICADO a "ya resiliente, no requiere RPC" tras verificación en código (2026-05-27)**: `generarFactura` ejecuta el CPE PRIMERO (`cpeIdempotencyKey`, idempotente) y el descuento de stock DESPUÉS, con guard de idempotencia en `aplicarSalidaStockFacturacionSimplificada` (verifica movimiento SALIDA existente por PEDIDO+producto antes de descontar, línea ~1828) usando la RPC atómica `descontar_stock_y_liberar_reserva`. Los UPDATEs de pedido/detalles son idempotentes en efecto. Un reintento converge (CPE reutilizado → stock skip → pedido actualizado). Una RPC SQL monolítica es **imposible** (el CPE es I/O externo SOAP/firma a SUNAT, no puede correr dentro de una transacción Postgres) e **innecesaria** (el flujo ya es un saga idempotente convergente). El riesgo original de la auditoría ("stock descontado sin factura") está mitigado por el orden CPE-primero + idempotencia. No se agrega RPC.
- **H-003 (confirmar pedido) — CERRADO 2026-05-27**: nueva migración `339__reservar_pedido_stock_transaccional.sql` con RPC `reservar_pedido_stock_tx(p_pedido_id, p_tenant_id)`. Reserva el stock de TODOS los items del pedido en una sola transacción reusando `reservar_stock_atomico` por item; si un item no tiene stock, la excepción aborta el statement y Postgres hace ROLLBACK total (libera las reservas ya creadas automáticamente) — elimina el rollback manual frágil que podía dejar el pedido CONFIRMADO con reserva parcial. Idempotente: si ya hay reservas para el pedido, retorna `skipped=true`. `pedidos.service.ts:confirmarPedido` refactorizado: reemplaza el bloque loop+rollback-manual (~80 líneas) por una llamada a la RPC, preservando la semántica de `saltarReserva` (ahora = `reservaResult.skipped`) para el revert best-effort posterior. Verificación: migración aplicada a DEV; smoke SQL real en 3 escenarios — (1) stock insuficiente en item 2 → excepción + 0 reservas en item 1 (rollback total OK, sin reserva parcial), (2) stock suficiente → ambos reservados + 2 movimientos, (3) idempotencia → 2da llamada skipped sin duplicar. tsc limpio; suite backend 991/992 en ese corte (el fallo `cpe-integration.verify.spec.ts` era pre-existente y quedo cerrado el 2026-06-16). Migración 339 aplicada a DEV **y PROD** (PROD verificado 2026-05-29: `reservar_pedido_stock_tx` presente, EXECUTE solo `service_role`).
- **Fase 1B COMPLETA**: C-004 (recepción, mig 338) + H-003 (confirmar pedido, mig 339) con RPC transaccional; H-002 (facturar) como saga idempotente reforzada. Migración `341__transactional_idempotency_coverage_hardening.sql` aplicada y verificada en DEV y PROD el 2026-06-01: `cerrar_recepcion_tx` ahora valida idempotencia por `recepcion_item_id`; `reservar_pedido_stock_tx` valida cobertura completa por `pedido_detalle_id`; ambas RPC mantienen EXECUTE solo para `service_role` (`anon=false`, `authenticated=false`, `service_role=true`). Además `stock.movimiento` se persiste con `eventId/idempotencyKey` determinísticos y H-002 repara `cantidad_facturada` en reintentos con `factura_id`.
- Auditoria forense full-scope adicional 2026-05-26 en `docs/audits/2026-05-26-system-analytics-completed.md`: mapa del sistema, hallazgos por severidad post-triage, matriz de integracion modulo-a-modulo, checklist preproduccion y 15 falsos positivos descartados con evidencia (`.env` NO esta en git, axios 1.16 no es vulnerable, Stripe SI valida HMAC, indices CxC/CxP SI existen, etc.). El complemento 2026-06-01 deja C-004/H-002/H-003 cerrados o mitigados con pruebas.
- **Auditoria de seguridad 2026-05-29 (rama `codex/accounting-production-closure`, diff completo vs `main`; foco auth/RLS/RPC SECURITY DEFINER):**
  - **H-1 (cross-tenant via RPC directa) — CERRADO:** `334` concedio `registrar_cxc_pago_tx`, `conciliar_movimientos_bancarios_tx` y `validar_tesoreria_caja_bancos_runtime` a `authenticated`; las 3 son `SECURITY DEFINER` y confian en el `p_tenant_id` del caller sin contrastarlo con `app.current_tenant_id()`. Pre-estado PROD confirmaba `authenticated=EXECUTE` (exposicion real explotable por RPC PostgREST directa). Cerrado con nueva migracion `340__securitydefiner_rpc_grant_hardening.sql`: `REVOKE` de `PUBLIC/anon/authenticated` + `GRANT` solo a `service_role` (alineado con 337/338/339). El backend ya invoca las 4 con `service_role`, no se rompe ningun flujo.
  - **H-2 (menor) — CERRADO:** `descontar_stock_y_liberar_reserva` (333/335) quedo `SECURITY DEFINER` sin GRANT explicito; pre-estado PROD ya tenia `authenticated=false` (defaults de Supabase revocan PUBLIC), exposicion practica nula. La `340` lo hace explicito/consistente. No deriva de `p_tenant_id` (toma el tenant del producto).
  - **Migracion 340 APLICADA Y VERIFICADA en DEV y PROD** (unica de la tanda 337..340 aplicada a PROD): las 4 funciones quedan `authenticated=f, anon=f, service_role=t` (verificado via `has_function_privilege`).
  - **Topologia auth (decidida con el usuario): web (Vercel) + API (Render/VPS) como SUBDOMINIOS del mismo root** (`app.<root>`/`api.<root>`, cookie `Domain=.<root>`) -> same-site, la cookie HttpOnly autentica los fetch y el middleware SSR funciona. Stage 1 en codigo: `auth.controller.ts` `setAuthCookie`/`clearAuthCookie` honran `AUTH_COOKIE_DOMAIN` (sin la env -> host-only, no-op en dev/desktop).
  - **M-1 (JWT en localStorage) — CERRADO 2026-07-14:** web usa cookie HttpOnly; Tauri mantiene Bearer en memoria y persiste solo mediante DPAPI fuera del WebView. Snapshots, demo, E2E y outbox no guardan JWT/password/Authorization.
  - **Middleware SSR no es frontera de control de acceso** (en cross-domain ni veria la cookie; en build Tauri/static-export ni corre). Frontera real = guards globales del backend (`JwtAuthGuard` default-deny + `PermissionGuard` + interceptores de tenant). Sin token/permiso valido no se sirve dato (401/403).
  - Verificacion 2026-07-14: `test:offline`, typecheck web y `cargo check` limpios; escaneo de patrones en archivos versionados sin JWT/private keys/provider keys reales.
- **Verificacion DeepSec seguridad 2026-06-04 (`docs/audits/2026-06-04-deepsec-security-verification.md`) — aplicada en codigo:** se verifico `.deepsec/data/erp/reports/report.md` contra el backend actual. Cerrados en esta pasada: CPE/GRE worker con `@Public()` + `WorkerAuthGuard` para no quedar bloqueados por `JwtAuthGuard` global; `revokeUserSessions()` invalida caches positivas `auth:session:<token>` antes de borrar sesiones; `resetPassword()` consume el token de forma atomica con update condicional por `password_reset_token`. Confirmados como ya cerrados antes: POS worker canonicaliza tenant y rechaza conflicto header/query, detalle POS usa parametro URL, apertura/cierre caja no confian en supervisor/sesion arbitrarios del cliente, logs POS no serializan payload completo, failed-login usa RPC atomica, JWT exige `session_token`. Residual aceptado: `/auth/refresh` no usa refresh token rotativo separado; se mantiene como deuda recomendada porque ya revalida sesion server-side y usuario fresco. Verificacion: `pnpm --filter @erp-suite/erp-api run test -- auth.service.spec.ts worker-auth.guard.spec.ts cpe.controller.spec.ts gre.worker.controller.spec.ts --runInBand` OK (49/49) y `pnpm --filter @erp-suite/erp-api run type-check` OK.
- **Hardening SUNAT SOAP directo 2026-06-16 — aplicado y con CDR beta aceptado para CPE 01/03/07/08:** `OseService` ya no hardcodea `billService` beta ni reutiliza otros CPE para GRE; resuelve endpoints por operacion (`CPE`, `GRE`, `summary/baja`, `query`) con defaults oficiales beta/produccion y overrides `SUNAT_CPE_URL`, `SUNAT_GRE_URL`, `SUNAT_SUMMARY_URL`, `SUNAT_QUERY_URL`. RA/RC usan `sendSummary`, tickets usan `getStatus` real y consulta de CDR usa `getStatusCdr`; el controlador separa envio/consulta de comunicaciones de baja y resumenes diarios. `SunatFiscalService` tambien usa aliases `SUNAT_USERNAME`/`SUNAT_PASSWORD` y endpoint de consulta CDR. `CpeService.generateXmlContent()` genera UBL 2.1 SUNAT para factura/boleta con `ProfileID`, `InvoiceTypeCode listID`, `cac:Signature`, `PaymentTerms`, precios de referencia, IGV por linea y totales legales; notas `07/08` generan `CreditNote`/`DebitNote` con `DiscrepancyResponse`, `BillingReference`, catalogos 09/10 y local anexo. Smoke beta real contra `https://e-beta.sunat.gob.pe/ol-ti-itcpfegem-beta/billService` usando `[RUC]MODDATOS`/`MODDATOS`: factura `20100066603-01-F001-50507464`, boleta `20100066603-03-B001-50578301`, nota de credito `20100066603-07-F001-13059219` y nota de debito `20100066603-08-F001-13059220` aceptadas con `responseCode=0`; ver hashes en evidencia. RA/RC dejaron de ser XML placeholder y `sendSummary` beta devolvio tickets RA/RC, pero `getStatus` beta devolvio fallos tecnicos `code=99` por XML incompleto; no se declaran RA/RC aceptados ni rechazados fiscalmente. GRE usa endpoint beta oficial de guia y falla cerrado; smoke GRE `20100066603-09-T001-37559129` fue rechazado por SUNAT beta con fault `2112`. Evidencia: `docs/audits/2026-06-16-sunat-beta-cpe-evidence.md`. Verificacion: `pnpm --filter @erp-suite/crypto run build` OK; `pnpm --filter @erp-suite/erp-api run test -- cpe.service.spec.ts comunicacion-baja.service.spec.ts gre.idempotency.spec.ts gre.service.spec.ts ose.service.spec.ts sunat-fiscal.service.spec.ts xml-signer-runtime.spec.ts --runInBand` OK (7 suites, 43 tests); `pnpm --filter @erp-suite/erp-api run type-check` OK.
- **Usuario SOL secundario SUNAT 2026-06-17 — creado y validado sin emision productiva:** `ERPFE001` aparece activo en SUNAT para RUC `20616053575`. La credencial secundaria fue validada con `getStatusCdr` read-only contra `https://e-factura.sunat.gob.pe/ol-it-wsconscpegem/billConsultService` usando comprobante inexistente `01-F001-99999999`: credencial real devolvio `HTTP 200/statusCode=0127`, clave falsa devolvio fault `La Clave ingresada es incorrecta`. Se corrigio el cliente SOAP para hosts `*.sunat.gob.pe`: WS-Security UsernameToken sin HTTP Basic; HTTP Basic queda solo para endpoints externos no SUNAT. `SUNAT_USERNAME`/`SUNAT_PASSWORD` ahora tienen precedencia sobre aliases legacy `OSE_*`. `.env` local queda con `SUNAT_ENVIRONMENT=homologacion` para evitar emision productiva accidental. El PFX `CT2604146559.pfx` fue cargado localmente como `certs/sunat-20616053575.pfx`, abre con clave privada, no es demo (`demoMode=false`) y firma XML local con `XmlSigner({ allowDemoFallback:false })`; sin embargo, SUNAT Orientacion indica que para persona juridica el certificado debe contener el RUC de la empresa y la RS 182-2016/SUNAT exige que el certificado tenga el numero de RUC del titular. Este subject visible corresponde a `GADEA LIRA ANGEL RICARDO CE:003581663`, no al RUC `20616053575`. Quedo implementado guard productivo: si `SUNAT_ENVIRONMENT=produccion`, el certificado debe contener `SUNAT_CERT_EXPECTED_RUC`/`EMPRESA_RUC`, o fallara antes de firmar/enviar salvo confirmacion explicita documentada. Ademas existe preflight no destructivo `sunat:readiness-preflight` que carga el PFX sin enviar a SUNAT y genera reporte sanitizado; la corrida local homologacion `docs/audits/artifacts/sunat-readiness-preflight/2026-06-17T07-42-54-611Z/manifest.json` confirma `productionUsed=false`, `rucMatches=false` y `canAttemptProductionSend=false`; la corrida forzada con `SUNAT_ENVIRONMENT=produccion` en `docs/audits/artifacts/sunat-readiness-preflight/2026-06-17T07-48-32-656Z/production-simulated-manifest.json` termino con exit code `1`, `certificate.ruc_match=FAIL`, `productionUsed=false` y `canAttemptProductionSend=false`. No enviar CPE de la empresa con este PFX hasta confirmacion escrita del proveedor/SUNAT o certificado emitido para el RUC 20. Consulta beta read-only de tickets RA/RC historicos devolvio `0127 El ticket no existe`. Luego se agrego y ejecuto runner reproducible `sunat:beta-smoke:ra-rc-gre` en homologacion: factura `20100066603-01-F001-39234513` y boleta `20100066603-03-B001-39234514` fueron aceptadas con CDR beta; RA `20100066603-RA-20260617-34513` devolvio ticket `1781679870729`; RC `20100066603-RC-20260617-34514` devolvio ticket `1781679882872`. La primera consulta `getStatus` de RA/RC devolvio `HTTP 401 Authorization Required`, pero el runner read-only `sunat:beta-ticket-readonly` recupero despues CDR beta aceptado para ambos: RA CDR SHA-256 `3D91CD861E2841DF84B4EC24FFD8BF215012C5AD9B829E9007C65BA214F1EB43` y RC CDR SHA-256 `0D1710B6437FA850CD3F2E30F47F795C1373ED45AE4B9F12DDB5D584E297FE76`. Una reconsulta posterior del mismo ticket devolvio `0127`, documentado como comportamiento de reuso/consulta posterior del beta, no como rechazo fiscal. GRE SOAP `20100066603-09-T001-39234515` y reintento GRE-only `20100066603-09-T001-24734262` fueron rechazados con fault `2112` aun cuando la validacion oficial vigente exige `CustomizationID=2.0` y el XML firmado lo contiene. Se implemento soporte opcional para Plataforma Nueva GRE REST (`SUNAT_GRE_TRANSPORT=rest`, `SUNAT_GRE_CLIENT_ID`, `SUNAT_GRE_CLIENT_SECRET`, token OAuth y envio a `api-cpe.sunat.gob.pe`). El preflight forzando GRE REST sin credenciales API falla cerrado con `gre.rest_credentials=FAIL`, `productionUsed=false`, `canAttemptProductionSend=false`; evidencia: `docs/audits/artifacts/sunat-readiness-preflight/2026-06-17T08-08-29-088Z/gre-rest-missing-credentials-manifest.json`. Falta registrar/obtener esas credenciales API SUNAT para cerrar GRE REST real si el contribuyente emitira guias. Evidencia y raw SOAP: `docs/audits/artifacts/sunat-beta-ra-rc-gre/2026-06-17T07-04-03-049Z-1489/`, `docs/audits/artifacts/sunat-beta-ticket-readonly/2026-06-17T07-56-24-782Z-3619/`, `docs/audits/artifacts/sunat-beta-ticket-readonly/2026-06-17T07-56-41-259Z-1286/`, `docs/audits/artifacts/sunat-beta-ra-rc-gre/2026-06-17T07-22-43-886Z-gre-only-8975/` y `docs/audits/artifacts/sunat-readiness-preflight/`; resumen: `docs/audits/2026-06-17-sunat-secondary-sol-evidence.md`. Verificacion: crypto build OK; guard/firma/SOAP 4 suites/32 tests OK; tests fiscales con guard 8 suites/63 tests OK; backend type-check OK; `git diff --check` OK.
- **Alineacion contable SUNAT/PCGE 2026-06-17 — aplicada en codigo:** PLE Libro Diario/Mayor exporta fechas `DD/MM/AAAA`, correlativo de linea con prefijo `M` y corte mensual estable; Libro Diario API usa solo `asientos_contables` canonico y evita duplicar `asientos_contables_rrhh`; generadores POS/RRHH/manuales/integracion legacy/apertura/cierre anual ya no escriben `numero_asiento` manual sino que delegan secuencia/codigo a BD; planillas registran aportes patronales reales en 621/627 contra 403/407/411; cierre POS queda idempotente por sesion; cierre anual queda balanceado en detalle con PCGE 89/59. Evidencia: `docs/audits/2026-06-17-sunat-accounting-entries-pos-rrhh-ple-alignment.md`. Verificacion: backend type-check OK; Jest focalizado 6 suites/65 tests OK. Pendiente externo: PLE/SIRE/PLAME real con data y regimen del contribuyente, mas revision contable/legal.
- **Regla boleta S/ 700 vs GRE 2026-06-17 — aplicada en codigo:** se corrigio la confusion historica que presentaba boleta mayor a S/ 700 como disparador de GRE. SUNAT exige consignar identificacion del adquirente/usuario; no exige GRE por ese monto. `boleta-validation`, `use-boleta-validation`, `BoletaGREWarning` y POS ahora muestran identificacion/regla logistica interna, no GRE legal por S/ 700. `SunatFiscalService.validarDocumento` ya no advierte factura menor a S/ 700 y valida RUC receptor de factura + identificacion en boleta mayor a S/ 700. Evidencia: `docs/audits/2026-06-17-pos-sunat-print-readiness.md` y `docs/audits/2026-06-17-web-runtime-devtools-audit.md`. Verificacion: `sunat-fiscal.service.spec.ts` 6/6 OK, web/backend type-check OK, navegador integrado local sin `warn/error` en login/rutas protegidas; tenant real sigue bloqueado por API/Supabase `ready=503`.
- **QA por roles web/API 2026-06-17 — cerrado para navegacion autenticada local:** tenant demo controlado `c0af84b5-5ea6-44a9-9e6e-a869f119b013` con `ADMIN_DEMO`, `ADMIN` y roles operativos `GERENCIA`, `COMPRAS`, `ALMACEN`, `VENDEDOR`, `CAJERO`, `FINANZAS`, `CONTADOR`, `RRHH`, `AUDITOR`. Matriz API: endpoint permitido `200` y endpoint denegado `403` por rol; `ADMIN_DEMO` sin `/users`, `ADMIN` con `/users`. Matriz UI: 11/11 rutas autenticadas cargan sin respuestas API `>=400`, sin `console.error/warning`, sin `pageerror` y sin texto de permiso denegado. Evidencia final: `docs/audits/artifacts/web-runtime-roles/role-ui-playwright-2026-06-17T21-41-38-834Z.json`. Pase adicional con navegador integrado visible y usuario `ADMIN` (`aprobador`) recorrio 56/56 rutas reales de modulos/submodulos con CDP (`Network`, `Runtime`, `Log`) y consola: 0 `warn/error`, 0 respuestas HTTP `>=400` relevantes, 0 `Network.loadingFailed`, 0 loaders persistentes tras revalidacion larga; artefacto `docs/audits/artifacts/web-runtime-admin-visible/admin-visible-cdp-2026-06-18T01-12-16-797Z.json`. Cambios asociados: auditoria por permiso `security.audit.read`, sidebar con permisos en padres, filtro de usuarios de auditoria sin `/users` para AUDITOR, CxC sin `/ventas/clientes` para FINANZAS, `GET /configuration/gre-thresholds` con `pos.read`, logout Bearer y POS sin `console.warn` por configuracion incompleta.
- **QA funcional con escrituras controladas 2026-06-18 — cerrado para alcance probado:** sobre el mismo tenant QA se ejecutaron flujos reales de escritura y verificacion de negocio: compras/proveedor/producto/recepcion/CxP, ventas/CxC/asientos, RRHH/planilla, contabilidad, POS con venta/pago/ticket/caja/cierre/corte/asiento y concurrencia idempotente. Se aplicaron `343` (locks RPC `SECURITY DEFINER`) y `344` (alias `cuentas_por_cobrar.total`) en BD activa QA/DEV y luego tambien en PROD cuando la conexion productiva fue revalidada. El smoke POS concurrente hizo 5 requests simultaneos con la misma `idempotency_key` y persistio una sola venta, stock 5->4, un solo movimiento, cierre con diferencia 0, corte y asiento `POS_CIERRE` con 3 lineas cuadradas. Load read-only autenticado: 287/287 OK, 0 HTTP 429/5xx. Ticket PDF 80mm generado sin SUNAT produccion. Evidencia: `docs/audits/2026-06-18-functional-e2e-qa-write-smoke.md`. Verificacion: backend type-check OK; `contabilidad-completo.spec.ts` OK post-344; artefactos en `docs/audits/artifacts/functional-e2e-qa/`. Sigue bloqueado para "100% produccion SUNAT" por certificado RUC 20/autorizacion PFX, credenciales GRE REST si aplica, impresora termica fisica, carga final en infraestructura destino y smoke productivo externo autorizado.

## Migraciones vigentes

- Reconstruccion base documentada: `000..305`.
- Gates remotos/manuales aplicados el 2026-05-16: `312..326`.
- Auditorias y cierres posteriores en repo local: `327..345`.
- `337__client_migration_rls_rpc_hardening.sql` (Codex), `338__cerrar_recepcion_transaccional.sql` (C-004) y `339__reservar_pedido_stock_transaccional.sql` (H-003) aplicadas a DEV **y PROD** (verificado 2026-05-29: objetos presentes, EXECUTE solo `service_role`, RLS forced en `migration_runs`). El doc previo las marcaba pendientes en PROD; la DB confirma que ya están.
- `340__securitydefiner_rpc_grant_hardening.sql` (auditoria seguridad 2026-05-29): restringe `EXECUTE` a `service_role` en las 3 RPC de tesoreria de `334` (H-1) y en `descontar_stock_y_liberar_reserva` (H-2). **Aplicada y verificada en DEV y PROD** (`authenticated=f, anon=f, service_role=t`).
- `341__transactional_idempotency_coverage_hardening.sql` (cierre Codex 2026-06-01): refuerza idempotencia/cobertura de `cerrar_recepcion_tx` y `reservar_pedido_stock_tx`. **Aplicada y verificada en DEV y PROD** (`anon=false`, `authenticated=false`, `service_role=true`, comentarios presentes).
- `342__sunat_tenant_onboarding_credentials.sql` (cierre SUNAT tenant 2026-06-17): agrega contrato persistente en `empresa_config` para `sunat_environment`, usuario/clave SOL secundaria, endpoints CPE/summary/query/GRE, transporte GRE SOAP/REST, credenciales OAuth GRE REST y guard documentado de mismatch RUC/certificado. **Aplicada y verificada en DEV y PROD** el 2026-06-18: 15 columnas presentes, 3 constraints validadas, defaults correctos y 0 filas inválidas en ambos entornos.
- `343__job_lock_rpc_security_definer_hardening.sql` (QA funcional 2026-06-18): endurece `acquire_job_lock`/`release_job_lock` como `SECURITY DEFINER`, fija `search_path`, restringe `EXECUTE` a `service_role` y corrige el retorno de lock cuando otro worker ya lo posee. **Aplicada y verificada en DEV y PROD**; smoke productivo no destructivo de locks: primer lock `true`, segundo lock simultaneo `false`, release `true`.
- `344__cxc_total_alias_runtime_alignment.sql` (QA funcional 2026-06-18): alinea `cuentas_por_cobrar.total` con `monto_total`, agrega backfill/normalizador y constraint de consistencia para que `registrar_cxc_pago_tx` opere contra el esquema activo. **Aplicada y verificada en DEV y PROD**; verificacion PROD: columna `total` existe y `cxc_total_mismatches=0`.
- `345__supabase_advisor_security_hardening.sql` (hardening Supabase Advisor 2026-06-18): fuerza RLS backend-only en `financial_forensic_repair_log`, convierte vistas normales de `public` a `security_invoker=true`, restringe vistas diagnosticas/runtime a `service_role`, fija `search_path` en funciones propias y revoca `EXECUTE` de funciones propias `SECURITY DEFINER` a `anon/authenticated`. **Aplicada y verificada en DEV y PROD**: `security_definer_views_remaining=0`, `rls_disabled_public_tables_remaining=0`, `user_functions_without_search_path=0`, `secdef_user_functions_client_executable=0`, `financial_forensic_anon_select=false`, `financial_forensic_authenticated_select=false`, `financial_forensic_service_role_select=true`.
- `360__metodos_pago_tipo_check_y_fallback_global.sql` (cierre de pendientes 2026-07-26): la resolución del medio de pago en la RPC del POS cae al catálogo global (`tenant_id IS NULL`) cuando el tenant no tiene filas propias, prefiriendo siempre la suya, y `metodos_pago.tipo` recibe CHECK de taxonomía (`EFECTIVO`, `TARJETA`, `TRANSFERENCIA`, `BILLETERA_DIGITAL`). **Aplicada y verificada sólo en DEV.**
- `359__metodos_pago_tipo_por_codigo.sql` (arqueo de caja 2026-07-26): realinea `metodos_pago.tipo` de las filas por tenant con la taxonomía del catálogo global de `024`, sólo para códigos inequívocos hoy marcados como efectivo. **Aplicada y verificada sólo en DEV** (78 filas).
- `358__pos_igv_por_afectacion_en_venta.sql` (cierre de totales POS 2026-07-26): `pos_registrar_venta_full_tx_legacy_327` y `pos_registrar_venta_tx` liquidan el IGV sumando el que trae cada ítem según su afectación del Catálogo 07, en vez de aplicar la tasa plana a todo el subtotal. La tasa plana queda sólo como compatibilidad si ningún ítem declara `igv`. Firma, numeración, stock, caja, outbox e idempotencia sin cambios. **Aplicada y verificada sólo en DEV.**
- `357__fiscal_correlativo_resync_documento_series.sql` (cierre numeración fiscal POS 2026-07-26): realinea `documento_series.correlativo_actual` de cada serie fiscal con el máximo correlativo ya emitido en `documentos`/`cpe`/`ventas_pos` y crea los contadores ausentes. Nunca baja un contador ni renumera documentos históricos. **Aplicada y verificada sólo en DEV**; PROD está vacía de tenants, por lo que allí es no-op cuando se promueva.
- La colision local de prefijo `333__` fue resuelta:
  - `333__inventory_stock_reconciliation_hardening.sql`: inventario/logistica/costeo.
  - `334__treasury_cash_bank_forensic_closure.sql`: tesoreria/caja/bancos/CxC/CxP.
  - `335__descontar_stock_authoritative.sql`: ajuste autoritativo posterior de salida de stock.
- Migracion de data desde ERP externo aplicada el 2026-05-26: `336__client_data_migration_external_id_and_audit.sql` agrega `external_id` UNIQUE(tenant_id, external_id) a clientes/proveedores/productos/CxC/CxP/asientos/cuentas_bancarias/plan_cuentas, crea tablas `migration_runs` y `migration_run_rows`, funcion `validar_migracion_apertura(tenant, fecha_corte)` con 6 checks, y permisos `migration.*` para rol ADMIN. Aplicada exitosamente en BD remota DEV (erp-dev) el 2026-05-26: 8/8 columnas external_id, 8/8 indices unicos parciales, 2/2 tablas auditoria con RLS, funcion validador retorna 6 checks correctamente. Tres bugs cazados al aplicarla:
  1. La columna `status` del RETURN TABLE colisionaba con `migration_runs.status` en 3 CTEs del validador → resuelto aliasando la tabla como `mr` y calificando `mr.status`, `mr.run_type`, `mr.tenant_id`.
  2. El check `ck_cuentas_por_cobrar_ids_required` exigia `documento_id NOT NULL` pero data migrada no tiene documento local → relajado para permitir `documento_id IS NULL` cuando `metadata->>'origen' = 'migracion_apertura'`.
  3. Los importers de CxC/CxP/balance_apertura no respetaban varios CHECKs existentes (estado `ACTIVO` en asientos contables debe ser `confirmado`; CxP estado `CANCELADO` no existe — debe ser `PAGADA`; faltaban `event_source`, `idempotency_key`, `condiciones_pago`, `numero_documento` como text) → corregidos en los services del modulo migration.
  Smoke E2E SQL contra dev valido los 6 checks del validador en dos escenarios (cuadrado: OK/SKIP; mismatch forzado: CHK_002 FAIL correctamente). Backend TS compila limpio en `src/modules/migration/*`; 31/31 tests unitarios siguen OK.
- Hardening forense local 2026-05-27: agregado `337__client_migration_rls_rpc_hardening.sql` para forzar RLS en `migration_runs`/`migration_run_rows`, corregir politicas a `app.current_tenant_id()` y revocar `validar_migracion_apertura` a `authenticated/anon` dejando ejecucion solo por `service_role`. Tambien se corrigio idempotencia de stock inicial por sucursal/almacen/fecha, validacion estricta de `fileBase64`, contrato de runTypes CSV, XSS en impresiones/toasts y tenant obligatorio en `DocumentosService`. Evidencia: `docs/production-readiness/ERP_FORENSIC_AUDIT_2026-05-27.md`; tests focalizados 34/34 OK y type-check backend/frontend OK.
- Intento de smoke API end-to-end (HTTP completo) el 2026-05-26 falló por blocker de infra: la API levantada localmente cargaba `app.module.ts` con `envFilePath: ['.env', 'apps/erp-api/.env']`, lo cual podia ignorar `.env.local` y apuntar a PROD (`wypnbcptofqdmoynlonq`) en pruebas locales. Daño operativo historico: `/api/demo/create` se ejecutó una vez contra PROD por error, creó tenant `495fa8ef-7a63-4089-a199-6bf5f33a6536` auto-suspendido — limpiado inmediatamente con `DELETE FROM public.tenants WHERE id='495fa8ef...'` (CASCADE), PROD regresó de 42 a 41 tenants. Migración 336 NUNCA se aplicó a PROD; solo a DEV. **Actualizacion 2026-06-17:** `app.module.ts` ahora usa `apiEnvFilePath` con precedencia `apps/erp-api/.env.local`, `.env.local`, `apps/erp-api/.env`, `.env` y test de regresion `env-files.spec.ts`; el riesgo de override local queda cerrado en codigo. El smoke autenticado sigue como follow-up externo porque los hosts Supabase configurados no resuelven DNS desde esta maquina (`*.supabase.co` para DEV/PROD devuelve NXDOMAIN), por lo que `/api/health/ready` queda 503 y no se puede validar tenant real localmente hasta restaurar/actualizar el proyecto Supabase o configurar entorno local.
- **Actualizacion 2026-06-18 sobre ese bloqueo:** la limitacion anterior aplica al smoke HTTP/API de esa fecha, no a la conexion directa de BD. PROD `.env.production` fue revalidado por `psql` contra el pooler y se aplicaron/verificaron `342/343/344/345`; el smoke API productivo externo sigue pendiente como control separado.
- Antes de aplicar o reconstruir BD, verificar siempre que no existan prefijos duplicados en `supabase/migrations`.

Comando de verificacion de prefijos en PowerShell:

```powershell
Get-ChildItem -Path supabase\migrations -Filter *.sql |
  Group-Object { $_.Name.Substring(0,3) } |
  Where-Object { $_.Count -gt 1 } |
  Select-Object Name,Count,@{Name='Files';Expression={($_.Group.Name -join ', ')}}
```

## Entornos Supabase

| Entorno | Proyecto | Ref | Cuenta | Host pooler | Archivo env |
|---|---|---|---|---|---|
| **PROD** | `erp` | `wypnbcptofqdmoynlonq` | yohandry10 | `aws-0-us-west-2.pooler.supabase.com` | `.env.production` |
| **DEV** | `erp-dev` | `hbueraexcbowpfnjlppi` | yohandrydev.1995 | `aws-1-us-west-2.pooler.supabase.com` | `.env.dev` |

- Actualizacion 2026-06-18: la conexion PROD de `.env.production` fue revalidada por `psql` contra `wypnbcptofqdmoynlonq` (Postgres 17.6) y se usó para aplicar/verificar `343`, `344` y `345`. El fallo previo `ENOTFOUND tenant/user postgres.wypnbcptofqdmoynlonq` queda como incidente historico, no como bloqueo vigente. Antes de cualquier nueva migracion en PROD, repetir siempre prueba `psql` read-only y aplicar con `ON_ERROR_STOP=1`.
- `erp-dev` creado 2026-05-25; 332/332 migraciones aplicadas; 22/22 validators OK (inventory 6/6, accounting 5/5, tesoreria 11/11).
- Para cambiar entorno: `Copy-Item .env.dev .env.local` (dev) o `Copy-Item .env.production .env.local` (prod).
- Supabase CLI instalado en `$env:USERPROFILE\supabase-cli\supabase.exe`. Token prod en cuenta yohandry10; token dev en cuenta yohandrydev.1995.
- NUNCA commitear `.env.dev`, `.env.production` ni `.env.local` (cubiertos por `*.env.*` en `.gitignore`).

## Fuentes canonicas por tema

| Tema | Fuente primaria | Notas |
|---|---|---|
| Primera lectura y navegacion | `docs/START_HERE.md` | Orden de lectura, jerarquia documental y rutas por tarea |
| Mapa completo documental | `docs/DOC_NAVIGATION_MANIFEST.md` | Cataloga Markdown y artefactos CSV/TXT/JSON; cada Markdown tiene cabecera `DOC-NAV` |
| Estado actual y siguiente sesion | `docs/00_coordination/CURRENT_STATE.md` | Fuente viva despues de `START_HERE.md` |
| Estado por flujo | `docs/00_coordination/FLOW_STATUS.md` | Matriz de cierre y pendientes |
| Reconstruccion BD base | `docs/db_rebuild_status.md` | Historico `000..305`; no usar solo para produccion |
| Readiness local/sandbox | `docs/production-readiness/ERP_PRODUCTION_READINESS.md` | Gate 21/22 y decision de no produccion real absoluta |
| Go-Live productivo | `docs/release/GO_LIVE_RUNBOOK.md` | Runbook ejecutable cuando el operador tenga credenciales reales |
| Migracion desde ERP externo | `docs/migration/CLIENT_MIGRATION_RUNBOOK.md`, `docs/production-readiness/ERP_FORENSIC_AUDIT_2026-05-27.md` | Modulo `apps/erp-api/src/modules/migration/` + migraciones `336__` y hardening `337__`. Cubre clientes/proveedores/CxC/CxP/balance apertura/stock inicial/CPE historico via CSV con `external_id`; `337` ya aplicado/verificado en DEV y PROD. Mejoras opcionales: importer dedicado para plan_cuentas/cuentas_bancarias y UI web. |
| Contabilidad/fiscal | `docs/auditoria_forense_contable_2026-05.md`, `docs/audits/2026-06-17-sunat-accounting-entries-pos-rrhh-ple-alignment.md` | Cierre tecnico, legal externo pendiente |
| Impresion CPE/facturas | `docs/auditoria_impresion_cpe_facturas_2026-05.md`, `docs/audits/2026-06-16-sunat-beta-cpe-evidence.md`, `docs/audits/2026-06-17-sunat-secondary-sol-evidence.md`, `docs/audits/2026-06-17-pos-sunat-print-readiness.md`, `docs/audits/2026-06-17-sunat-cpe-gre-xml-alignment.md` | Auditoria/remediacion 2026-05-25; factura, boleta, nota de credito y nota de debito SUNAT beta aceptadas con CDR `0` el 2026-06-16; usuario SOL secundario `ERPFE001` validado por `getStatusCdr` read-only el 2026-06-17; PFX real no-demo cargado y firma XML local OK. POS reforzado el 2026-06-17: ticket interno si CPE pendiente y representacion CPE con QR SUNAT + valor resumen/hash cuando existe `cpe_id`; 80mm es criterio operativo termico, no medida SUNAT obligatoria documentada. Runner beta RA/RC/GRE ejecutado el 2026-06-17: RA/RC reciben tickets y reconsulta read-only recupera CDR beta aceptado para ambos; GRE SOAP rechaza `CustomizationID=2.0` con `2112` incluso en reintento firmado sin atributos. Backend soporta Plataforma Nueva GRE REST, pendiente de credenciales API SUNAT. Alineacion XML/contrato 2026-06-17: CPE preserva fecha/hora local sin corrimiento UTC; GRE emite partida en `Delivery/Despatch/DespatchAddress`, exige conductor/placa en transporte privado y RUC transportista en publico; UI captura esos datos. Pendiente: confirmar autorizacion del PFX para el RUC, credenciales/smoke GRE REST si aplica, smoke con impresora/navegador real y produccion controlada. |
| App desktop/Tauri | `docs/auditoria_desktop_vs_web_2026-05.md`, `apps/web/README-DESKTOP.md` | Online-first + offline local aplicado 2026-05-25: `offline_mode` fuerza cache/outbox, cola Tauri con lock local, sync usa API vigente, sidebar con prefetch limitado/escalonado, build/export/Tauri debug OK; 108/108 rutas exportadas smoke OK con API simulada; backend API sigue siendo autoritativo |
| Multiusuario/performance | `docs/auditoria_multiusuario_performance_2026-05.md` | Mitigados cuellos de polling/retries no idempotentes y crons multi-instancia con locks distribuidos; prueba read-only real 589/589 OK, p95 1490 ms, p99 1956 ms, 0 HTTP 429/5xx; outbox remoto limpio sin pending/processing/failed/dead_letter; falta prueba productiva completa con escrituras controladas |
| Inventario/logistica/costeo | `docs/auditoria_forense_inventario_logistica_costeo_2026-05.md` | Cierre `333` y ajuste `335` |
| Tesoreria/caja/bancos/CxC/CxP | `docs/auditoria_forense_tesoreria_caja_bancos_cxc_cxp_2026-05.md` | Cierre `334` |
| Operacion Supabase | `docs/ops/supabase-connection.md` | Aplicacion manual por `psql` y notas remotas |
| Seguridad/rutas | `docs/security/route-access-matrix.md` | Matriz vigente de autorizacion por endpoint |
| Docs historicas | `docs/DOCUMENTATION_QUARANTINE.md` y `docs/archive/` | Consultar como contexto, no como verdad unica |

## Pendientes reales

Bloqueados por dependencias externas (no estan en alcance de codigo; ver `docs/release/GO_LIVE_RUNBOOK.md` para la secuencia exacta cuando esten disponibles):

- Confirmar por escrito que el PFX real no-demo cargado (`CT2604146559.pfx`, thumbprint `74EA9ABFF4BB8A7A515FDAA0443632E1635D06FA`) esta autorizado para firmar CPE del RUC `20616053575`, o reemplazarlo por un certificado de persona juridica que contenga el RUC 20. El usuario SOL secundario `ERPFE001` ya esta creado/validado y no debe reemplazarse por la clave principal; no pegar claves SOL en tickets/docs/chat.
- Completar matriz fiscal externa: CPE factura `01`, boleta `03`, nota de credito `07` y nota de debito `08` ya tienen CDR beta aceptado; RA/RC ya generan XML, reciben ticket beta y tienen CDR beta aceptado por reconsulta read-only; usuario SOL secundario `ERPFE001` ya fue validado; PFX real no-demo ya firma XML localmente; guard productivo bloquea certificados sin RUC esperado; GRE SOAP beta queda no concluyente por `2112` aun con XML `CustomizationID=2.0` correcto; GRE REST esta cableado y falla cerrado sin `SUNAT_GRE_CLIENT_ID`/`SUNAT_GRE_CLIENT_SECRET`, por lo que requiere credenciales API SUNAT para probar Plataforma Nueva GRE REST si el contribuyente emitira guias; falta smoke productivo controlado con CDR/acuse real despues de resolver el certificado del RUC 20.
- Antes de distribuir desktop: smoke del ejecutable contra API real, validar cola offline con reconexion real en el `.exe`, configurar `ALLOWED_ORIGINS`/CSP final y decidir si se requiere paridad completa de deep links dinamicos con refresh/acceso directo.
- Cargar secretos productivos finales y proveedor real de email si aplica.
- Repetir preflight `psql` read-only antes de nuevas migraciones PROD aunque `.env.production` ya fue revalidado el 2026-06-18; `342/343/344/345` ya no estan pendientes en PROD.
- Ejecutar smoke fiscal externo con CPE/GRE/SIRE/PLE/PLAME segun alcance del contribuyente.
- Ejecutar prueba de capacidad productiva completa con mas concurrencia y escrituras controladas/idempotentes contra API/Supabase reales: POS, ventas, compras, CPE, inventario y finanzas.
- Auth cross-subdominio: configurar custom domains `app.<root>` + `api.<root>`, `ALLOWED_ORIGINS`, `AUTH_COOKIE_DOMAIN=.<root>` y secretos coherentes; verificar que la cookie HttpOnly autentique navegador/middleware. Web no tiene fallback persistente a JWT; Tauri usa su Bearer protegido con DPAPI.

- Mantener monitoreo de outbox, CPE sin asiento, SIRE/PLE vs mayor, pagos sujetos a bancarizacion y divergencias de inventario.

Cerrados en sesion 2026-05-24:

- ~~Revalidar `327..335` con `psql --set=ON_ERROR_STOP=1` en una BD nueva/limpia~~ -> revalidacion completa de `000..335` ejecutada el 2026-05-24, 22/22 validadores OK.
- ~~Reconciliar 3 productos descuadrados + 14 salidas sin costo pre-`333`~~ -> backfill aplicado el 2026-05-24, validador inventario quedo 6/6 OK.
- ~~SEC-001: `/compras/ordenes/:id/aprobar` aceptaba `aprobador_id` en body ignorando el JWT~~ -> el DTO ya no expone `aprobador_id`, el service usa SIEMPRE el `userId` del JWT, runtime confirmado: la API responde `400 - property aprobador_id should not exist`. Helper `apiContextAsAprobador()` agregado en `apps/web/tests/e2e/helpers/test-data.ts` para que los e2e autentiquen el aprobador con su propio JWT. 7 specs e2e + 1 backend e2e migrados; `inventario-logistica.spec.ts` revalidado end-to-end (2.1m OK). Requiere env vars nuevas `TEST_APROBADOR_EMAIL` y `TEST_APROBADOR_PASSWORD` al correr e2e.
- ~~BUG-001: POST `/api/cpe` directo no generaba asiento contable~~ -> el `ContabilidadEventsListener` ahora se suscribe a `factura.emitida` (que ya emitia `CpeService`); el cron lo persiste en outbox y `handleFacturaEmitida` genera el asiento de venta a partir del payload. Idempotencia por `(tenant + referencia serie-numero)`: si `venta.procesada` ya creo el asiento (flujo /ventas/pedidos/:id/facturar) el handler skipea sin duplicar. Validado: Jest backend 953/953 (subio de 951 por 2 tests SEC-001 nuevos); e2e `cpe-completo` paso 1.5m OK con handler activo; listener registra `factura.emitida` al boot del API (visible en logs).
- ~~Outbox dead_letter historico 2026-05-20 (`venta.procesada`, `B001-00000001`)~~ -> cerrado el 2026-05-26 como reconciliado, sin reprocesar, porque ya existia asiento confirmado y cuadrado para la misma referencia por flujo fiscal/CxC (`source_event_id=6cfb9a36-76f2-43de-807e-f0e42ccd72b2`). Revalidado: `completed=3985`, `pending=0`, `processing=0`, `failed=0`, `dead_letter=0`; referencia `B001-00000001` mantiene 1 asiento por 47.20/47.20.

## Estado de aplicacion 327..335 en BD remota (verificado 2026-05-24)

Verificacion read-only via `psql --dbname="$POSTGRES_URL"` contra `wypnbcptofqdmoynlonq`: 28 artefactos clave (funciones, indices, tablas, vistas) presentes; `331__production_accounting_flow_hardening.sql` se detecto NO aplicada (faltaban `app.seed_operational_defaults_for_tenant` y `ux_conceptos_planilla_tenant_codigo`) y se aplico limpia el mismo dia.

| Migration | Estado | Notas |
|---|---|---|
| 327 | Aplicada | `pos_registrar_venta_full_tx` + idx idempotency |
| 328 | Aplicada | Uniques de sesiones de caja |
| 329 | Aplicada | `create_demo_tenant` con fix pgcrypto |
| 330 | Aplicada | RBAC demo admin + expiry idx |
| 331 | Aplicada 2026-05-24 | Faltaba en remoto; aplicada limpia (`BEGIN`..`COMMIT`, 0 errores; trigger fiscal `tenants`, 24 filas updated) |
| 332 | Aplicada | Tabla `normativa_peru_periodos` + validador compliance |
| 333 | Aplicada | Validador inventario + view de status |
| 334 | Aplicada | Tabla `financial_forensic_repair_log` + RPC `registrar_cxc_pago_tx` + `conciliar_movimientos_bancarios_tx` |
| 335 | Aplicada | `descontar_stock_y_liberar_reserva` con descuento autoritativo (verificado via `pg_get_functiondef`) |

Validadores runtime ejecutados post-`331` y post-backfill residual (2026-05-24):

- `validar_accounting_production_compliance_runtime(NULL)`: **5/5 OK**.
- `validar_tesoreria_caja_bancos_runtime(NULL)`: **11/11 OK**.
- `validar_inventory_stock_reconciliation_runtime(NULL)`: **6/6 OK** (era 4/6 antes del backfill; los 3 productos descuadrados y 14 salidas pre-`333` quedaron cerrados con UPDATE puntual).

## Revalidacion 000..335 en BD limpia (2026-05-24)

Ejercicio de "linea canonica desde cero" ejecutado contra Postgres 16 limpio en Docker (`erp-pg-revalidate` en `:15432`, descartado al final):

1. Pre-requisitos minimos creados antes de aplicar: extensions `pgcrypto` y `uuid-ossp`; esquemas `app` y `auth`; tabla skeleton `auth.users`; roles `anon`, `authenticated`, `service_role` (BYPASSRLS). Estos prerequisitos son los que Supabase provee de fabrica y no estan declarados en las migraciones; se documentan aqui para reproducir el despliegue en un Postgres no-Supabase.
2. Aplicacion en orden de las 332 migraciones disponibles (huecos historicos `006..009`, max `335`) con `psql --dbname --set=ON_ERROR_STOP=1 -q -f`. Todas aplicaron limpio sin errores.
3. Validadores runtime sobre la BD reconstruida: **22/22 OK** (inventory 6/6, accounting compliance 5/5, tesoreria 11/11).

Conclusion: las 332 migraciones forman una linea canonica reproducible sobre Postgres 16. Cualquier despliegue futuro a un Supabase nuevo solo necesita los prerequisitos del paso 1 (los gestiona Supabase por defecto en proyectos nuevos).

## Protocolo de nueva sesion

1. Ejecutar `git status --short`.
2. Leer `docs/START_HERE.md`.
3. Leer `docs/00_coordination/CURRENT_STATE.md`, `docs/00_coordination/FLOW_STATUS.md` y `docs/00_coordination/AGENT_SYNC.md`.
4. Leer `docs/DOC_NAVIGATION_MANIFEST.md` si la tarea toca documentacion, auditorias o seleccion de fuentes.
5. Verificar duplicados de prefijo en `supabase/migrations` antes de tocar BD.
6. Si se toca BD, leer baseline forense y plan de reconstruccion antes de cualquier cambio.
7. Si se toca inventario, revisar `333__inventory_stock_reconciliation_hardening.sql`, `335__descontar_stock_authoritative.sql` y la auditoria de inventario.
8. Si se toca tesoreria/caja/bancos/CxC/CxP, revisar `334__treasury_cash_bank_forensic_closure.sql` y el handoff.
9. Si se toca produccion/release, revisar readiness, production checklist y ops Supabase.
10. No usar manuales de modulo como estado final sin contrastar con auditorias de mayo 2026 y este archivo.

## Protocolo de cierre de tarea

Antes de responder como terminado:

1. Revisar si la tarea cambio estado global, estado de flujo, migraciones, riesgos, pendientes o navegacion documental.
2. Si hubo cambio, actualizar `docs/00_coordination/CURRENT_STATE.md` y/o `docs/00_coordination/FLOW_STATUS.md`.
3. Actualizar tambien el documento fuente del flujo afectado.
4. Actualizar `docs/START_HERE.md` si cambia el resumen ejecutivo, jerarquia documental o rutas de lectura.
5. No tocar estos archivos si la tarea fue local y no cambia estado ni contexto compartido.
