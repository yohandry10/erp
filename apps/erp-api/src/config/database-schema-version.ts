/**
 * Esquema mínimo con el que este binario fue construido y probado.
 *
 * La variable de despliegue puede exigir una versión posterior, pero nunca
 * debe rebajar este piso: una configuración rezagada en Render no puede hacer
 * que readiness acepte un runtime incompatible con la base contractual.
 */
export const MINIMUM_DATABASE_SCHEMA_VERSION = 532;
